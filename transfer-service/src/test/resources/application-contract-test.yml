spring:
  cloud:
    loadbalancer:
      enabled: true
    bootstrap:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false

stubrunner:
  cloud:
    loadbalancer:
      enabled: true
  ids: 'ru.ugaforever.bank.notification:notification-service:+:stubs:0'
  stubsMode: LOCAL

